header:
  - titles:
      # @start locale config
      en      : &EN       Projects
      fr      : &FR       Projets
      # @end locale config
    url: /projects

  - titles:
      # @start locale config
      en      : &EN       Archive
      fr      : &FR       Archives
      # @end locale config
    url: /archive

  - titles:
      # @start locale config
      en      : &EN       About
      fr      : &FR       À propos
      # @end locale config
    url: /about
  